function wifi_toggle() {
    export wifi_status=$(nmcli radio wifi)
    export backup_file=~/.cache/airplane_backup

    if [ "$wifi_status" = enabled ]; then
        nmcli radio wifi off
    else
        nmcli radio wifi on
        if test -e $backup_file; then
            rm $backup_file
        fi
    fi
}
